# .render.yaml
services:
    - type: web
      name: default-risk
      env: python
      plan: free
      region: frankfurt
      branch: masterS
      buildCommand: "pip install -r requirements.txt"
      startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
      workingDir: back-end
      envVars: []
